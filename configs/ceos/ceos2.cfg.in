hostname ceos2
no aaa root
!
aaa authentication policy local allow-nopassword-remote-login
aaa authorization exec default local
username admin nopassword role network-admin privilege 15
!
service routing protocols model multi-agent
!
vrf instance MGMT
!
! Default gateway for custom mgmt subnet (Containerlab bridge).
ip route vrf MGMT 0.0.0.0/0 ${MGMT_GATEWAY}
!
interface Management0
   vrf MGMT
   ip address ${MGMT_IP_CEOS2}/${MGMT_PREFIX}
!
interface Ethernet1
   no switchport
   ip address 10.255.0.2/30
   mac security profile dynamic
   dot1x pae supplicant macsec-sp
!
interface Ethernet2
   no switchport
   ip address 10.0.2.254/24
!
ip routing
ipv6 unicast-routing
!
ip route 10.0.1.0/24 10.255.0.1
!
management api gnmi
   transport grpc default
      vrf MGMT
      ssl profile GNMI
!
management api restconf
   transport https restconf
      ssl profile RESTCONF
   vrf MGMT
!
management api eos-sdk-rpc
   transport grpc default
      ssl profile GNMI
!
management api netconf
   transport ssh default
      vrf MGMT
!
management api http-commands
   no shutdown
   protocol https ssl profile EAPI
   !
   vrf MGMT
      no shutdown
!
management ssh
   key-exchange mlkem768x25519-sha256
   cipher aes256-gcm@openssh.com aes128-gcm@openssh.com chacha20-poly1305@openssh.com
   mac hmac-sha2-256 hmac-sha2-512
   shutdown
   !
   vrf MGMT
      no shutdown
      !
!
! RadSec/eAPI/gNMI/RESTCONF PKI is installed post-boot via containerlab exec (copy is exec-only, not startup-config).
management security
   ssl profile EAPI
      tls versions 1.3
      key-establishment-group X25519MLKEM768:ecdh_x25519:secp256r1
      cipher v1.3 TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:TLS_AES_128_GCM_SHA256
      certificate ceos2-eapi.pem key ceos2-eapi.key
   !
   ssl profile GNMI
      tls versions 1.3
      key-establishment-group X25519MLKEM768:ecdh_x25519:secp256r1
      cipher v1.3 TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:TLS_AES_128_GCM_SHA256
      certificate ceos2-gnmi.pem key ceos2-gnmi.key
      trust certificate radsec-ca.pem
   !
   ssl profile RESTCONF
      tls versions 1.3
      key-establishment-group X25519MLKEM768:ecdh_x25519:secp256r1
      cipher v1.3 TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:TLS_AES_128_GCM_SHA256
      certificate ceos2-gnmi.pem key ceos2-gnmi.key
      trust certificate radsec-ca.pem
   !
   ssl profile RADSEC
      tls versions 1.3
      key-establishment-group X25519MLKEM768:ecdh_x25519:secp256r1
      cipher v1.3 TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:TLS_AES_128_GCM_SHA256
      certificate ceos2-client.pem key ceos2-client.key
      trust certificate radsec-ca.pem
   !
   ssl profile DOT1X
      tls versions 1.3
      key-establishment-group X25519MLKEM768:ecdh_x25519:secp256r1
      cipher v1.3 TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:TLS_AES_128_GCM_SHA256
      certificate ceos2-client.pem key ceos2-client.key
      trust certificate radsec-ca.pem
!
radius-server host ${RADIUS_SERVER_IP} vrf MGMT tls ssl-profile RADSEC
!
aaa group server radius RADIUS
   server ${RADIUS_SERVER_IP} tls vrf MGMT
!
dot1x system-auth-control
!
dot1x
   supplicant profile macsec-sp
      identity ceos2
      eap-method tls
      ssl profile DOT1X
!
mac security
   profile dynamic
      key source dot1x
!
end
