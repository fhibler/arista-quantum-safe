# RadSec NAS clients (TLS, port 2083). Rendered to lab/.gen/clients-radsec.conf.

client ceos1-both {
    ipv6addr = ${MGMT_IPV6_CEOS1_BOTH}
    proto   = tls
    secret  = radsec
    nas_type = other
    require_message_authenticator = true
    limit_proxy_state = true
}

client ceos2-pqc {
    ipv6addr = ${MGMT_IPV6_CEOS2_PQC}
    proto   = tls
    secret  = radsec
    nas_type = other
    require_message_authenticator = true
    limit_proxy_state = true
}

client ceos3-qkd {
    ipv6addr = ${MGMT_IPV6_CEOS3_QKD}
    proto   = tls
    secret  = radsec
    nas_type = other
    require_message_authenticator = true
    limit_proxy_state = true
}
