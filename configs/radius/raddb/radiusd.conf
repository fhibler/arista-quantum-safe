# Lab log override — included from the packaged radiusd.conf at image build time.
# Mounted at /etc/raddb/radiusd-log.conf in the running container.
log {
    destination = files
    file = /var/log/radius/radius.log
}
