# Slim runtime base for lab images. Service Dockerfiles add their own packages.
# Keep this tiny: no python, tshark, docker-cli, or -dev packages.

ARG ALPINE_VERSION=3.24

FROM alpine:${ALPINE_VERSION}

RUN apk add --no-cache ca-certificates tzdata openssl
